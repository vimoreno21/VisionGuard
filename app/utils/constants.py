MATCH_THRESHOLD = 0.4  # Lower values = stricter matching
HEADLESS = True  # Set to True if running without display (e.g. in Docker)
SAVE_ALL_FRAMES = False  # Save frames periodically even without faces
FRAME_SAVE_INTERVAL = 10  # Save a frame every 5 seconds